<nav class="fixed top-0 left-0 w-full z-50 bg-white/95 backdrop-blur-lg border-b border-gray-200 shadow-lg" id="main-nav">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-3 sm:py-4">
        <div class="flex justify-between items-center">
            <a href="/" class="flex items-center gap-2 sm:gap-3 group transform hover:scale-105 transition-all duration-300">
                <div class="relative flex items-center justify-center h-12 w-12 sm:h-16 sm:w-16 rounded-full overflow-hidden">
                    
                    <div class="absolute inset-0 bg-gradient-to-r from-green-400 to-blue-500 rounded-full blur-sm opacity-20 group-hover:opacity-40 transition-opacity"></div>
                    
                    
                    <img 
                        src="/Hesed-Core-logo.png" 
                        alt="Hesed Core Logo" 
                        class="relative h-full w-full object-cover shadow-sm"
                        style="aspect-ratio: 1 / 1;"
                    />
                </div>
                
                
                <div class="flex items-center gap-1 sm:gap-2">
                    <span class="text-2xl sm:text-3xl font-black tracking-tight gradient-text">
                        Hesed
                    </span>
                    <span class="text-2xl sm:text-3xl font-normal text-gray-700 hidden sm:inline">
                        Core
                    </span>
                </div>
            </a>

            
            <div class="hidden md:flex items-center gap-8">
                <a href="#productos" class="relative font-semibold text-gray-700 hover:text-green-600 transition-colors duration-300 group">
                    Productos
                    <span class="absolute -bottom-1 left-0 w-0 h-0.5 bg-gradient-to-r from-green-400 to-blue-500 group-hover:w-full transition-all duration-300"></span>
                </a>
                <a href="#sobre-nosotros" class="relative font-semibold text-gray-700 hover:text-green-600 transition-colors duration-300 group">
                    Sobre Nosotros
                    <span class="absolute -bottom-1 left-0 w-0 h-0.5 bg-gradient-to-r from-green-400 to-blue-500 group-hover:w-full transition-all duration-300"></span>
                </a>
                <a href="#equipo" class="relative font-semibold text-gray-700 hover:text-green-600 transition-colors duration-300 group">
                    Equipo
                    <span class="absolute -bottom-1 left-0 w-0 h-0.5 bg-gradient-to-r from-green-400 to-blue-500 group-hover:w-full transition-all duration-300"></span>
                </a>
                
                <a href="https://wa.me/5218135731273" target="_blank" rel="noreferrer noopener" class="bg-gradient-to-r from-green-500 to-blue-600 text-white px-4 lg:px-6 py-2.5 rounded-full font-bold hover:from-green-600 hover:to-blue-700 transition-all duration-300 transform hover:scale-105 shadow-md hover:shadow-xl inline-flex items-center gap-2 lg:gap-3 text-sm lg:text-base">
                    <span class="inline-flex items-center justify-center rounded-full bg-white p-1.5">
                        <img src="/image.png" alt="WhatsApp" class="w-4 h-4 object-contain" />
                    </span>
                    <span>Solicitar Demo</span>
                </a>
            </div>

            
            <button id="menu-toggle" class="md:hidden p-3 rounded-2xl border border-gray-200 bg-white shadow-sm hover:bg-gray-100 transition-colors focus:outline-none focus:ring-2 focus:ring-green-500" aria-label="Menu">
                <svg class="w-6 h-6 text-gray-700" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path id="menu-icon" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
                    <path id="close-icon" class="hidden" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
                </svg>
            </button>
        </div>
    </div>

    <!-- Mobile Menu -->
    <div id="mobile-menu" class="hidden md:hidden bg-white border-b border-gray-200 absolute w-full left-0 top-full shadow-xl max-h-[calc(100vh-4rem)] overflow-y-auto">
        <div class="px-4 sm:px-6 py-6 flex flex-col gap-4">
            <a href="#productos" class="mobile-link text-lg sm:text-xl font-semibold text-gray-700 py-3 rounded-2xl hover:bg-gray-100 transition-colors px-4">Productos</a>
            <a href="#sobre-nosotros" class="mobile-link text-lg sm:text-xl font-semibold text-gray-700 py-3 rounded-2xl hover:bg-gray-100 transition-colors px-4">Sobre Nosotros</a>
            <a href="#equipo" class="mobile-link text-lg sm:text-xl font-semibold text-gray-700 py-3 rounded-2xl hover:bg-gray-100 transition-colors px-4">Equipo</a>
            <a href="https://wa.me/5218135731273" target="_blank" class="block w-full text-center bg-gradient-to-r from-green-500 to-blue-600 text-white px-6 py-4 rounded-2xl font-bold shadow-lg hover:from-green-600 hover:to-blue-700 transition-all duration-300">
                Solicitar Demo
            </a>
        </div>
    </div>
</nav>

<script>
    const menuToggle = document.getElementById('menu-toggle');
    const mobileMenu = document.getElementById('mobile-menu');
    const menuIcon = document.getElementById('menu-icon');
    const closeIcon = document.getElementById('close-icon');
    const mobileLinks = document.querySelectorAll('.mobile-link');

    function toggleMenu() {
        if (!mobileMenu || !menuIcon || !closeIcon) return;

        mobileMenu.classList.toggle('hidden');
        menuIcon.classList.toggle('hidden');
        closeIcon.classList.toggle('hidden');

        if (!mobileMenu.classList.contains('hidden')) {
            document.body.style.overflow = 'hidden';
        } else {
            document.body.style.overflow = 'auto';
        }
    }

    if (menuToggle) {
        menuToggle.addEventListener('click', toggleMenu);
    }

    mobileLinks.forEach(link => {
        link.addEventListener('click', () => {
            if (!mobileMenu || !menuIcon || !closeIcon) return;

            mobileMenu.classList.add('hidden');
            menuIcon.classList.remove('hidden');
            closeIcon.classList.add('hidden');
            document.body.style.overflow = 'auto';
        });
    });
</script>